<%= form_for([@story, @story.novels.build]) do |f| %>

  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>
  <p>
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </p>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
